import type { Metadata } from 'next';
import { ContactForm } from '@/components/ContactForm';
import { Mail, Sparkles } from 'lucide-react';
export const metadata:Metadata={title:'Contact',description:'Contact OGE-ACTRA TECH about technology, AI and digital platforms.'};
export default async function Contact({searchParams}:{searchParams?:Promise<{subject?:string}>}){const params=searchParams?await searchParams:{};return <section className="contact-page"><div className="contact-backdrop"/><div className="page-shell relative"><div className="contact-layout"><div><p className="eyebrow">Contact</p><h1 className="mt-4 text-5xl font-black tracking-[-.055em] md:text-7xl">Let&apos;s Build <span className="gradient-text">Something.</span></h1><p className="mt-6 max-w-xl text-xl leading-9 text-silver">Have an idea, product conversation or collaboration in mind? Tell OGE-ACTRA TECH what you want to build.</p><div className="mt-10 grid gap-3 sm:grid-cols-2"><div className="contact-info"><Mail size={18} className="text-cyan"/><div><span>Project conversations</span><small>Technology, products & partnerships</small></div></div><div className="contact-info"><Sparkles size={18} className="text-purple"/><div><span>Future opportunities</span><small>Careers & collaboration</small></div></div></div></div><ContactForm initialSubject={params.subject ?? ''}/></div></div></section>}
